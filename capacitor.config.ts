import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.pushapp',
  appName: 'pushapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
